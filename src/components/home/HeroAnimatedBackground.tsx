"use client";

import { AnimatedBlobField } from "@/components/home/AnimatedBlobField";

/** Dark hero shell; same motion as `AnimatedBlobField` `lightPoster` variant */
export function HeroAnimatedBackground() {
  return <AnimatedBlobField variant="hero" />;
}
